(set-logic QF_UF)
(declare-sort U 0)
(declare-const global U)
(declare-const other U)
(define-fun read-global ((argument U)) U global)
(assert (distinct global other))
(assert (let ((global other))
  (= (read-global global) global)))
(check-sat)
