# T5 446b Review: Linux Diagnostic Pass, WMI No-Go

Date: 2026-07-15

## Scope

Reviewed exact commit
`446b424ae270735f1296a40d3d8c21286de0d611`, sole parent
`0ad84317b5cf714785e6129d8403772c813e7758`. The checkout, strict object check,
20-file inventory, syntax checks, and 375-test bounded matrix were clean.

## Closed Boundaries

- The campaign now requires the external 7,503-row `smtlib-2025` manifest and
  explicitly rejects the tracked 3,521-row official manifest by path, digest,
  and cardinality.
- Linux publication verifies procfs fd semantics, uses `O_TMPFILE`, publishes
  through a descriptor with no replacement, sets mode `0444`, fsyncs the
  directory, and requires one link plus matching bytes.
- Runtime records include procfs, capability sets, statfs/mount identity,
  Python executable and stdlib, mapped libraries, OS/libc, and Slurm versions.
- Final scheduler evidence includes job ID, SLUID, cluster, submit time, name,
  user, work directory, state, and exit code. Unique attempts, exact Git blobs,
  transitive project imports, and stale marker handling are checked.

## Blocking Findings

1. Both `t5_independent_smtlib.py` and `independent_qfuf.py` copy caller-local
   bindings while expanding `define-fun`. A macro parameter shadowing a global
   constant yields the same incorrect equality in both implementations. Their
   agreement is therefore not semantic independence.
2. The remote setup releases the held census before the returned receipt is
   parsed, validated, and durably persisted locally. A local persistence
   failure can leave the full job running without an owned cancellation path.
3. Hosted CI provides no 7,503-source corpus, so the named end-to-end test
   skips. Its provisioned driver covers analyzer, independent verifier,
   finalizer, and consumer semantics but supplies synthetic scheduler evidence;
   it is not a live `sacct` end-to-end test.
4. Hosted action tags, runner image, and Python selection are not yet immutable
   evidence identities.

## Hosted Diagnostic

Review allowed exact-SHA publication only for Linux diagnostics. Branch
`research-t5-component-quotient-census-recovered` advanced to `446b424` and run
`29388947138` passed in about two minutes. The Linux publication step and root
Rust tests were green. The result confirms hosted procfs/filesystem behavior for
that test, not the 7,503-source semantic pipeline or Slurm accounting contract.

## Decision

No merge, tag, census, WMI canary, timing interpretation, or T5 authorization.
Fix macro lexical scope independently in both parsers, scan the exact corpus
before evidence, retain cancellation ownership until local receipt persistence
and separate release, make Linux publication diagnostics mandatory, label the
provisioned semantic pipeline accurately, and obtain another independent
review.
